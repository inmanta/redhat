{% for route in routes %}# Route {{ loop.index0 }}
ADDRESS{{ loop.index0 }}={{ route.network | cm("route_network", loop.index0, "net ref") }}
NETMASK{{ loop.index0 }}={{ route.netmask | cm("route_network", loop.index0, "net ref") }}
GATEWAY{{ loop.index0 }}={{ route.gateway | cm("route_network", loop.index0, "net ref") }}
{% endfor %}
