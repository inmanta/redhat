# Ensure that the name of the interface does not change
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", ATTR{address}=="{{iface.mac | std.cm("interface_mac", "host") }}", ATTR{type}=="1", KERNEL=="eth*", NAME="{{iface.name  | std.cm("interface_name", "host") }}"

