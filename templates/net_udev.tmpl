# Ensure that the name of the interface does not change
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", ATTR{address}=="{{iface.mac | cm("interface_mac", "host") }}", ATTR{type}=="1", KERNEL=="eth*", NAME="{{iface.name  | cm("interface_name", "host") }}"

